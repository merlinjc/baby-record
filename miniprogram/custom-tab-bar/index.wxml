<view class="tabbar-shell safe-bottom">
  <view class="tabbar-frame">
    <tabbar-item wx:for="{{list}}" wx:key="pagePath" item="{{item}}" index="{{index}}" active="{{selected === index}}" bind:tap="switchTab"></tabbar-item>
  </view>
</view>
